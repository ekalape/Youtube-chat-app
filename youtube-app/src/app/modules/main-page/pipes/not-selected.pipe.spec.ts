import { NotSelectedPipe } from './not-selected.pipe';

describe('NotSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new NotSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
