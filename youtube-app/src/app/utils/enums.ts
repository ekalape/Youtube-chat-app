export enum TimeDistanceColor {
  OLDIEST = '#F8312F',
  OLD = '#FFB02E',
  MEDIUM = '#00D26A',
  NEW = '#00A6ED',
}

export enum SortingRule {
  DATE_UP, DATE_DOWN, VIEWS_UP, VIEWS_DOWN,
}

export enum Pathes {
  BASE = "/",
  MAIN = "/main"
}
